# Fluid Objects（流体对象）目录总结

本目录是 Plant Simulation 帮助文档中 **Fluid Objects** 对象类的分类目录，包含以下对象类型，每个对象类型下均按 general / methods / attributes / read-only-attributes 等分页并以 `README.md` 总结。

## 对象一览

| 对象 | 说明 |
|---|---|
| `ContinuousMixer` | 连续混合器（ContinuousMixer） |
| `DePortioner` | 反分配器（DePortioner） |
| `FluidDrain` | 流体排出（FluidDrain） |
| `FluidSource` | 流体源（FluidSource） |
| `MaterialsTable` | 材料表（MaterialsTable） |
| `Mixer` | 混合器（Mixer） |
| `PatchMatrix` | 补丁矩阵（PatchMatrix） |
| `Pipe` | 管道（Pipe） |
| `Portioner` | 分配器（Portioner） |
| `Shared Properties` | 共享属性（Shared Properties） |
| `Tank` | 储罐（Tank） |

## 目录说明

- 每个对象子目录内含对应的 `.md` 源文件（部分含 `.txtx` 文本提取文件）与 `README.md` 总结。
- 本 README 为分类索引，具体内容见各对象子目录。

*来源：Plant Simulation Help — "Fluid Objects"。Unpublished work. © 2026 Siemens.*
