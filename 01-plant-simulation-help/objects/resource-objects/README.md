# Resource Objects（资源对象）目录总结

本目录是 Plant Simulation 帮助文档中 **Resource Objects** 对象类的分类目录，包含以下对象类型，每个对象类型下均按 general / methods / attributes / read-only-attributes 等分页并以 `README.md` 总结。

## 对象一览

| 对象 | 说明 |
|---|---|
| `AGVPool` | AGV 池（AGVPool） |
| `Broker` | 经纪人（Broker） |
| `Exporter` | 导出器（Exporter） |
| `FootPath` | 步道（FootPath） |
| `LockoutZone` | 锁定区域（LockoutZone） |
| `Marker` | 标记（Marker） |
| `ShiftCalendar` | 班次日历（ShiftCalendar） |
| `Worker` | 工人（Worker） |
| `WorkerPool` | 工人池（WorkerPool） |
| `Workplace` | 工作场所（Workplace） |

## 目录说明

- 每个对象子目录内含对应的 `.md` 源文件（部分含 `.txtx` 文本提取文件）与 `README.md` 总结。
- 本 README 为分类索引，具体内容见各对象子目录。

*来源：Plant Simulation Help — "Resource Objects"。Unpublished work. © 2026 Siemens.*
