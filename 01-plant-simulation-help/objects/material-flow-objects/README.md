# Material Flow Objects（物料流对象）目录总结

本目录是 Plant Simulation 帮助文档中 **Material Flow Objects** 对象类的分类目录，包含以下对象类型，每个对象类型下均按 general / methods / attributes / read-only-attributes 等分页并以 `README.md` 总结。

## 对象一览

| 对象 | 说明 |
|---|---|
| `AngularConverter` | 角度转换器（AngularConverter） |
| `AssemblyStation` | 装配工位（AssemblyStation） |
| `Buffer` | 缓冲区（Buffer） |
| `Connector` | 连接器（Connector） |
| `Converter` | 转换器（Converter） |
| `Conveyor` | 输送机（Conveyor） |
| `Cycle` | 循环（Cycle） |
| `DismantleStation` | 拆解工位（DismantleStation） |
| `Drain` | 排出（Drain） |
| `EventController` | 事件控制器（EventController） |
| `FlowControl` | 物料流控制（FlowControl） |
| `Frame` | 框架（Frame） |
| `Interface` | 接口（Interface） |
| `ParallelStation` | 并行工位（ParallelStation） |
| `PickAndPlace Robot` | 拾放机器人（PickAndPlace Robot） |
| `PlaceBuffer` | 放置缓冲区（PlaceBuffer） |
| `Sorter` | 分拣机（Sorter） |
| `Source` | 源（Source） |
| `Station` | 工位（Station） |
| `Store` | 仓库（Store） |
| `Track` | 轨道（Track） |
| `Turnplate` | 转盘（Turnplate） |
| `Turntable` | 转台（Turntable） |
| `TwoLaneTrack` | 双车道轨道（TwoLaneTrack） |

## 目录说明

- 每个对象子目录内含对应的 `.md` 源文件（部分含 `.txtx` 文本提取文件）与 `README.md` 总结。
- 本 README 为分类索引，具体内容见各对象子目录。

*来源：Plant Simulation Help — "Material Flow Objects"。Unpublished work. © 2026 Siemens.*
